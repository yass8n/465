<p id="notice"><%= notice %></p>
<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>User:</strong>
  <%= @post.user.username %>
</p>

<p>
  <strong>Content:</strong>
  <%= @post.content %>
</p>

<% @comments.each do |comment| %>
	<div> <%= comment.user.username + " " + comment.created_at.strftime("Created on %m/%d/%Y at %I:%M%p") + " " + comment.comment %> </div>
	<div class ="center"><%= render partial: "ratings/rating", locals: {comment: comment} %></div>
<% end %>
<br>

<div class ="center"><%= render 'comments/form' %></div>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
